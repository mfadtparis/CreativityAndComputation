size (1000, 1000);

for(int i = 0; i < width; i+=10){
  // repeats while i is less than the width
  // line (i, 0, i, height);

for(int j = 0; j < height; j+=10){
  stroke(i,j,0);
  line( i, j, i+10, j+10);

}
}
